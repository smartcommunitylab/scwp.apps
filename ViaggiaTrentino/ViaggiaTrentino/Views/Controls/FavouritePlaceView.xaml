<UserControl x:Class="ViaggiaTrentino.Views.Controls.FavouritePlaceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vtautocomp="clr-namespace:ViaggiaTrentino"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />     
    </Grid.ColumnDefinitions>
    <vtautocomp:GoogleAutoCompleteBox Grid.Column="0" x:Name="googleACB" FilterMode="Contains" ItemTemplate="{StaticResource googleBox}"/>
      <Image Grid.Column="1" x:Name="GpsLocTo" Source="/Assets/Buttons/GpsLocation.png" Margin="0,0,5,0" Tap="GpsLocTo_Tap"/>
  </Grid>
</UserControl>